spring:
  application:
    name: gateway

  cloud:
    nacos:
      # 注册中心
      discovery:
        server-addr: localhost:8848

      # 配置中心
      config:
        server-addr: localhost:8848
        file-extension: yaml